import React from 'react';
import { Text } from 'react-native';

import { BaseView } from './styles';

export const Home = () => {
  return (
    <BaseView>
      <Text>HOME</Text>
    </BaseView>
  );
};
